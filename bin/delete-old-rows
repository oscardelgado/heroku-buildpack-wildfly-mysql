#!/usr/bin/env ruby
require "sequel"
DB = Sequel.connect ENV["DATABASE_URL"]
puts "Deleting old rows..."
dataset = DB["delete from heroku_a304a837b2c7d20.ExportPOJO where updateTimestamp < DATE_SUB(curdate(), INTERVAL 1 MONTH) and accountName <> 'test'"]
puts "done. Deleted #{dataset.count} rows"
